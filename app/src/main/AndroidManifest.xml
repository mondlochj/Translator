<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Required for TranslationService and LLMService foreground type on API 34+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Required for TTSService foreground type (audio output to BT headset in background) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Keeps CPU alive when screen is off so transcription/inference continues uninterrupted -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- NNAPI hardware acceleration feature declaration -->
    <uses-feature android:name="android.hardware.neural_networks" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:name=".MeetingAssistantApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArosysMeetingAssistant"
        android:largeHeap="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.TranscriptionService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <!-- dataSync type satisfies API 34 requirement for inference services with no mic/camera/location -->
        <service
            android:name=".services.TranslationService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- mediaPlayback type required for audio output to Bluetooth headset when screen is off -->
        <service
            android:name=".services.TTSService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <service
            android:name=".services.LLMService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

    </application>

</manifest>
